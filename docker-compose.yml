version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: capstoneimg:latest
    ports:
      - "8003:8000"
    command: ["npm", "start"]
    working_dir: /app
