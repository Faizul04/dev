version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: capstoneimg:latest
    ports:
      - "2000:8000"
    command: ["npm", "start"]
    working_dir: /app

